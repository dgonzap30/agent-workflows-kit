---
paths:
  - "**/*.native.tsx"
  - "**/*.native.jsx"
  - "**/mobile/**"
  - "**/react-native/**"
---

# React Native

- Use safe-area-aware layout and test changed interactions on the relevant device or simulator when available.
- Size touch targets and labels for the platform context, including accessibility settings where the change affects them.
- List optimization is a tradeoff: profile the actual list and use stable layouts or rendering strategies only when their assumptions hold.
